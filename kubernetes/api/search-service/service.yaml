apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: reservainteligente
spec:
  selector:
    app: search-service
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
