# Service de Metabase. UI en puerto 3000, accesible via port-forward.
apiVersion: v1
kind: Service
metadata:
  name: metabase
  namespace: reservainteligente
spec:
  selector:
    app: metabase
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
